## Conditional syntax(if, cond, or, and) Chckers
### IF
| name | explain | implemented |
| ---- | ---- | ---- |
| valid-if | Detecting invalid if expression syntax. | ✅ |
| always-true-if |  Detecting cases where the test of an if expression is always true. | ✅ |
